/* eslint-env mocha */

import { Meteor } from 'meteor/meteor';

if (Meteor.isServer) {
  describe('Messaging', () => {
    describe('methods', () => {
      it('can store a new message', () => {

      });
    });
  });
}
